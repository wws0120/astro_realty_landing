import AOS from 'aos';
AOS.init({
  duration: 1000, // match your animation duration
  easing: 'cubic-bezier(0.645, 0.045, 0.355, 1)', // match your easing function
  once: true, // animation occurs only once
});
